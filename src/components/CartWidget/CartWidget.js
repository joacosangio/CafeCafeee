import { Icon } from '@iconify/react';

const CartWidget = () => {
    return (
        <div>
            <Icon className='cart-icon' icon="akar-icons:cart" />
        </div>
    )
}

export default CartWidget;